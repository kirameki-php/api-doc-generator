{varType array $tree}
<ul>
  {foreach $tree as $name => $leaf}
    <li>
      {if is_array($leaf)}
        <div class="sidebar-folder">{$name}</div>
        {include sidebar.latte, tree: $leaf}
      {else}
        {varType Kirameki\ApiDocGenerator\ClassInfo $leaf}
        <a href="{$leaf->getHtmlPath()}" target="content">{$leaf->basename}</a>
      {/if}
    </li>
  {/foreach}
</ul>
